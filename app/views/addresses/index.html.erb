<p style="color: green"><%= notice %></p>

<% content_for :title, "Addresses" %>

<h1>Addresses</h1>

<div id="addresses">
  <% @addresses.each do |address| %>
    <%= render address %>
    <p>
      <%= link_to "Show weather forecast", address, class: 'btn btn-sm btn-primary' %>
    </p>
  <% end %>
</div>

<%= link_to "New address", new_address_path %>
